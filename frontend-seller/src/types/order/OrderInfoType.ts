export interface OrderInfoType {
    orderInfoId: number;
    orderNo: string;
    orderContents: string;
    orderPrice: number;
    orderStatus: string;
    orderDate: string;
    consumerPhoneNumber: string;
}